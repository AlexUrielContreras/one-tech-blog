<!DOCTYPE html>
<html lang="en">

<head>
    <title>One Tech Blog</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="description" content="" />
    <link rel="stylesheet" href="/css/index.css" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Nunito:wght@500&family=Roboto:wght@300&display=swap">
</head>

<body>
    <div class="main-wrapper">
        <header>
            <a href='/'><h1 class="sm-style-main-title">One Tech Blog</h1></a>
            <nav>
                {{#if loggedIn}}
                <a href='/profile/{{username}}'>Profile</a>
                <button type="button" class="logout-btn">Logout</button>
                {{else}}
                <a href='/login'>Login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
        <footer>
            
        </footer>
    </div>
     {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
    {{/if}}
</body>
</html>