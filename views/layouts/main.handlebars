<!DOCTYPE html>
<html lang="en">

<head>
    <title>One Tech Blog</title>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <meta name="description" content="" />
    <link rel="stylesheet" href="/css/index.css" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Nunito:wght@500&family=Roboto:wght@300&display=swap">
</head>

<body>
    <div class="main-wrapper">
        <header>
            <h1 class="sm-style-main-title">
                <a href='/'>One Tech Blog</a>
            </h1>
            <nav>
                {{#if loggedIn}}
                    <a href='/profile/{{username}}'>Profile</a>
                    <button type="button" class="logout-btn">Logout</button>
                {{else}}
                    <a href='/login' class="login-btn">Login</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
    </div>
     {{#if loggedIn}}
        <script src="/javascript/logout.js"></script>
    {{/if}}
</body>
</html>