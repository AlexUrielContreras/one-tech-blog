<artical>
    <div class="post-title">
        <h2>{{ formatTitle title }}</h2>
        <span class="font-span">by {{ user.username }} {{formatDate createdAt}}</span>
    </div>
    <div class="post-body">
        <p>{{ post_info }}</p>
    </div>
</artical>